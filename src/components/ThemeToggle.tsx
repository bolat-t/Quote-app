import React from 'react';

interface ThemeToggleProps {
    size?: number;
}

// Theme toggle is no longer needed since we use a hardcoded potato-themed design.
export const ThemeToggle: React.FC<ThemeToggleProps> = () => {
    return null;
};
